name: statibackend_dart_client
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.2.0-134.1.beta

# Add regular dependencies here.
dependencies:
  http: ^1.1.2
  # path: ^1.8.0

dev_dependencies:
  build_runner: ^2.4.7
  lints: ^2.1.0
  mockito: ^5.4.4
  test: ^1.24.0
